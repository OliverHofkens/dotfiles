[user]
	name = Oliver Hofkens
	email = oliver@gorilla.co

[core]
    editor = nvim
    ignorecase = false

[pull]
    rebase = true
[merge]
	tool = kdiff3
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[push]
	autoSetupRemote = true
