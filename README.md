Most dotfiles are installable with stow.

The only exception is iterm, for which you need to point iterm to the preferences file directly. Otherwise it overwrites the symlink when you change a setting.
